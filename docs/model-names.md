# Model names

Use one canonical Gemini model identifier across README, examples, and config.

- Prefer the exact API model string for your provider version.
- Do not mix hyphen/underscore variants in the same project.
- Override via env var when examples need a different model; document the default here.
